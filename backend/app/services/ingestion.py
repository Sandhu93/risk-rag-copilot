class IngestionService:
    """Placeholder ingestion service for docs/tables."""

    def ingest_documents(self, input_path: str) -> dict[str, int]:
        # TODO: parse docs, chunk text, create embeddings, and persist index.
        return {"ingested_files": 0, "chunks_created": 0}
